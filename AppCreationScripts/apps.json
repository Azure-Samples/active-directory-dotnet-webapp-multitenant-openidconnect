{
  "Sample": {
    "Title": "Build a multi-tenant SaaS web application using Azure AD & OpenID Connect",
    "Level": 200,
    "Client": ".NET 4.6.1 Web App (MVC)",
    "Service": "ASP.NET Web API"
  },

  /*
    This section describes the Azure AD Applications to configure, and their dependencies
  */
  "AADApps": [
    {
      "Id": "service",
      "Name": "active-directory-dotnet-webapp-multitenant-openidconnect",
      "IsPublicClient": false,
      "HomePage": "https://localhost:44302/",
      "ReplyUrls": "https://localhost:44302/",
      "LogoutUrl": "https://localhost:44322/Account/EndSession",
      "PasswordCredentials": "Auto",
      "IsAvailableToOtherTenants":true,
      "RequiredResourcesAccess": [
        {
          "Resource": "Microsoft Graph",
          "DelegatedPermissions": [ "User.Read" ]
        }
      ]
    }
  ],

  /*
    This section describes how to update the code in configuration files from the apps coordinates, once the apps
    are created in Azure AD.
    Each section describes a configuration file, for one of the apps, it's type (XML, JSon, plain text), its location
    with respect to the root of the sample, and the mappping (which string in the config file is mapped to which value
  */
  "CodeConfiguration": [
    {
      "App": "service",
      "SettingKind": "XML",
      "SettingFile": "\\..\\dotnet-webapp-multitenant-openidconnect\\Web.Config",
      "Mappings": [        
        {
          "key": "ida:ClientId",
          "value": ".AppId"
        }
      ]
    }    
  ]
}
